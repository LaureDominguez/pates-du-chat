<?php if (isset($_SESSION['visitor']['flash_message']['success']) && !$_SESSION['visitor']['flash_message']['success']) : ?>
    <?php foreach ($success as $msg): ?>
    <div class="success">
        <?= htmlspecialchars($msg); ?>
    </div>
    <?php endforeach ?>
<?php endif; ?>

<?php if (isset($errors)) : ?>
    <div class="error">
        <?= htmlspecialchars($errors); ?>
    </div>
<?php endif; ?>