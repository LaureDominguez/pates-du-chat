<!-- <h3>Ajouter une image</h3> -->
<form id="imgForm" action="index.php?route=submitImgForm" method="post" enctype="multipart/form-data">
    <div id="imgInput" class="imgForm">
        <label for="img">image</label>
        <div class="imgField">
            <div>
                <input type="file" class="file" name="img" id="img">
                <button class="file-btn" type="submit" value="submit" form="imgForm">charger</button>
            </div>
            <p>(l'ancienne image sera remplacée par la nouvelle.)</p>
        </div>

        <input type="hidden" id="productId" name="productId" value="<?= $product['id']; ?>">

        <div class="gallery">
            <!-- changer table images pour faire une gallerie, lister chaque image liée au produit ci-dessous -->
            <div class="picture">
                <img class="productImage" id="productImage" src="public/img/produits/<?= $product['image']; ?>" alt="<?= $product['imgTitle']; ?>" data-id="<?= $product['imgId']; ?>" data-name="<?= $product['image']; ?>">
                <div class="number">1</div>
                <button type=" button" class="delete-btn" id="delete-btn"><i class="fa-solid fa-trash"></i></button>
            </div>

            <div id="imgMsg"></div>
        </div>


</form>