<div class="log" id="log-window">
    <button class="croix-btn" id="close-log">X</button>
    <h2>Connexion</h2>

    <form action="index.php?route=connect" method="post" id="loggin">
        <div>
            <label for="email_log">email</label>
            <input type="email" name="email" id="email_log">
        </div>
        <div>
            <label for="pswd_log">mot de passe</label>
            <input type="password" name="pswd" id="pswd_log">
        </div>

        <?php if (isset($_SESSION['visitor']['message']) && count($_SESSION['visitor']['message']) > 0): ?>
            <aside class="form-errors" id="error-log">
                <p>Mécréaaaaant !!!</p>
                <?php foreach ($_SESSION['visitor']['message'] as $alert): ?>
                    <p><?= htmlspecialchars($alert); ?></p>
                <?php endforeach; ?>
            </aside>
        <?php endif; ?>

        <div class="buttons">

            <button type="submit" class="btn" id="validate-log">valider</button>

            <div>
                <label>Pas de compte ?</label>
                <p class="btn" id="new-account">Créer un compte</p>
            </div>
        </div>
    </form>
</div>