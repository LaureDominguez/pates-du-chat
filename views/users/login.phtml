<div class="log noScroll" id="log_window">
    <i class="fa-solid fa-x close-btn" id="close-log"></i>
    <h2>Connexion</h2>

    <form action="index.php?route=connect" method="post" id="login-form">
        <div class="champs">
            <label for="email_log">email</label>
            <input type="email" name="email" id="email_log" autocomplete="email">
            <div class="error" id="error_log_mail">

                <?php if (isset($_SESSION['visitor']['flash_message']['error']['login'])) : ?>
                    <span id="errorL">
                        <?= htmlspecialchars($_SESSION['visitor']['flash_message']['error']['login'][0]); ?>
                    </span>
                <?php endif; ?>
            </div>
        </div>
        <div class="champs password">
            <label for="pswd_log">mot de passe</label>
            <div>
                <input type="password" name="pswd" id="pswd_log">
                <i class="fa-solid fa-eye" id="login-show"></i>
                <i class="fa-regular fa-eye" id="login-hide"></i>
            </div>
            <div class="error" id="error_log_pswd"></div>

            <div class="btn" id="forgot">Mot de passe oublié ?</div>
            <div class="message" id="forgot-message"></div>
        </div>

        <button type="submit" class="btn" id="validate-log">valider</button>
    </form>

    <a href="#" class="btn-flip" data-back="Créer un compte" data-front="Pas de compte ?" id="register"></a>

</div>