<div class="log" id="log_window">
    <button class="croix-btn" id="close-log">
        <i class="fa-solid fa-xmark"></i>
    </button>
    <h2>Connexion</h2>

    <form action="index.php?route=connect" method="post" id="loggin">
        <div>
            <label for="email_log">email</label>
            <input type="email" name="email" id="email_log">
            <?php if (isset($_SESSION['visitor']['msg']['log_email_errors'])) : ?>
                <?php foreach ($_SESSION['visitor']['msg']['log_email_errors'] as $error) : ?>
                    <div class="error" id="error_log_mail"><?= $error; ?></div>
                <?php endforeach; ?>
            <?php endif; ?>
        </div>
        <div>
            <label for="pswd_log">mot de passe</label>
            <input type="password" name="pswd" id="pswd_log">
            <?php if (isset($_SESSION['visitor']['msg']['log_pswd_errors'])) : ?>
                <?php foreach ($_SESSION['visitor']['msg']['log_pswd_errors'] as $error) : ?>
                    <div class="error" id="error_log_pswd"><?= $error; ?></div>
                <?php endforeach; ?>
            <?php endif; ?>
            <div class="password">
                <div>
                    <input type="checkbox" name="show_pass" class="checkbox" id="show_pass">
                    <label for="show_pass">Afficher le mot de passe</label>
                </div>
                <a href="">Mot de passe oublié ?</a>
            </div>
        </div>

        <button type="submit" class="btn" id="validate-log">valider</button>
    </form>

    <div class="flex">
        <p>Pas de compte ?</p>
        <a class="btn" id="new-account">Créer un compte</a>
    </div>
</div>