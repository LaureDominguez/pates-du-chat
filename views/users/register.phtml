<div class="log" id="register-window">
    <button class="croix-btn" id="close-register">
        <i class="fa-solid fa-xmark"></i>
    </button>
    <h2>Nouveau compte</h2>

    <form action="index.php?route=register" method="post">
        <div>
            <label for="email_new">email</label>
            <input type="email" name="email" id="email_new">
            <?php if (isset($_SESSION['visitor']['msg']['new_email_errors'])) : ?>
                <?php foreach ($_SESSION['visitor']['msg']['new_email_errors'] as $error) : ?>
                    <div class="error" id="error_new_mail"><?= $error; ?></div>
                <?php endforeach; ?>
            <?php endif; ?>
        </div>
        <div>
            <label for="pswd_new">mot de passe</label>
            <input type="password" name="pswd" id="pswd_new">
            <?php if (isset($_SESSION['visitor']['msg']['new_pswd_errors'])) : ?>
                <?php foreach ($_SESSION['visitor']['msg']['new_pswd_errors'] as $error) : ?>
                    <div class="error" id="error_new_pswd"><?= $error; ?></div>
                <?php endforeach; ?>
            <?php endif; ?>
        </div>
        <div>
            <label for="pswd_confirm_new">confirmez votre mot de passe</label>
            <input type="password" name="pswd_confirm" id="pswd_confirm_new">
            <?php if (isset($_SESSION['visitor']['msg']['new_verif_errors'])) : ?>
                <?php foreach ($_SESSION['visitor']['msg']['new_verif_errors'] as $error) : ?>
                    <div class="error" id="error_new_verif"><?= $error; ?></div>
                <?php endforeach; ?>
            <?php endif; ?>
        </div>

        <div class="buttons">
            <button type="submit" class="btn" value="Valider" id="validate-register">valider</button>
            <p class="btn" id="cancel">annuler</p>
        </div>
    </form>
</div>